{
  "max_errors": 0,
  "command": ".\\.venv\\Scripts\\mypy.exe --config-file pyproject.toml src/vamos",
  "notes": "mypy is currently clean on src/vamos (mypy 1.19.1); keep this at 0 to prevent regressions."
}
