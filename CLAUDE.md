# Claude Code adapter

Read and follow [the canonical repository instructions](AGENTS.md) at the start of the task. For a file below a nested `AGENTS.md`, read that file as a local delta and then use the single developer guide routed from the root contract.

Use repository searches to verify symbols and call sites before editing. Keep tool output and changes scoped, preserve unrelated worktree state, and report validation rather than assuming it passed. This adapter adds no project rules and cannot override `AGENTS.md`.

```agent-docs
path: AGENTS.md
```
