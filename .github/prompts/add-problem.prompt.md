# Add a VAMOS problem

Read [/AGENTS.md](/AGENTS.md), the local problem instructions, and [Adding a problem](/docs/dev/add_problem.md). Add the requested problem through the current family registry, with vectorized evaluation, encoding-aware tests, documentation, and the guide's targeted validation. Verify every touched symbol and path against the current checkout.
