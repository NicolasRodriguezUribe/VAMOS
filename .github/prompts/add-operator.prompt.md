# Add a VAMOS operator

Read [/AGENTS.md](/AGENTS.md), the local operator instructions, and [Adding an operator](/docs/dev/add_operator.md). Add the requested operator through the current implementation registry and policy boundary, preserving explicit RNG ownership, encoding invariants, vectorization, and the guide's focused tests.
