# Historical sample outputs

These CSV files are sanitized examples from an internal, non-public 0.1.0
development build. They are retained only as historical experiment inputs and
must not be interpreted as results from the public VAMOS 1.0.0 release.

Run paths are repository-relative placeholders. Reproduce public claims from a
pinned environment and newly generated canonical run manifests rather than
these pre-public samples.
